<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profit Report - {{formatDate report.startDate}} to {{formatDate report.endDate}}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #9b59b6;
            padding-bottom: 20px;
        }

        .company-info {
            margin-bottom: 20px;
        }

        .report-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            border-left: 4px solid #9b59b6;
        }

        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            margin-bottom: 25px;
        }

        .kpi-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
            text-align: center;
        }

        .kpi-revenue {
            border-left: 4px solid #3498db;
        }

        .kpi-cost {
            border-left: 4px solid #e74c3c;
        }

        .kpi-profit {
            border-left: 4px solid #27ae60;
        }

        .kpi-margin {
            border-left: 4px solid #9b59b6;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .table th,
        .table td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        .table th {
            background-color: #9b59b6;
            color: white;
        }

        .table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .positive-profit {
            color: #27ae60;
            font-weight: bold;
        }

        .negative-profit {
            color: #e74c3c;
            font-weight: bold;
        }

        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1 style="color: #9b59b6; margin-bottom: 10px;">PROFIT REPORT</h1>
        <div class="company-info">
            <h2 style="color: #2c3e50; margin: 0;">{{company.name}}</h2>
            <p style="margin: 5px 0;">{{company.address}}</p>
            <p style="margin: 5px 0;">Phone: {{company.phone}} | Email: {{company.email}}</p>
        </div>
    </div>

    <div class="report-info">
        <p><strong>Report Period:</strong> {{formatDate report.startDate}} to {{formatDate report.endDate}}</p>
        <p><strong>Generated On:</strong> {{generatedAt}}</p>
        {{#if report.generatedBy}}<p><strong>Generated By:</strong> {{report.generatedBy}}</p>{{/if}}
    </div>

    {{#if report.data.summary}}
    <div class="kpi-grid">
        <div class="kpi-item kpi-revenue">
            <h3 style="margin: 0; color: #2c3e50;">Total Revenue</h3>
            <p style="font-size: 24px; font-weight: bold; color: #3498db; margin: 10px 0;">${{formatCurrency
                report.data.summary.totalRevenue}}</p>
        </div>
        <div class="kpi-item kpi-cost">
            <h3 style="margin: 0; color: #2c3e50;">Total Cost</h3>
            <p style="font-size: 24px; font-weight: bold; color: #e74c3c; margin: 10px 0;">${{formatCurrency
                report.data.summary.totalCost}}</p>
        </div>
        <div class="kpi-item kpi-profit">
            <h3 style="margin: 0; color: #2c3e50;">Total Profit</h3>
            <p style="font-size: 24px; font-weight: bold; color: #27ae60; margin: 10px 0;">${{formatCurrency
                report.data.summary.totalProfit}}</p>
        </div>
        <div class="kpi-item kpi-margin">
            <h3 style="margin: 0; color: #2c3e50;">Avg Margin</h3>
            <p style="font-size: 24px; font-weight: bold; color: #9b59b6; margin: 10px 0;">{{formatPercentage
                report.data.summary.averageProfitMargin}}%</p>
        </div>
    </div>

    <div class="kpi-grid" style="grid-template-columns: repeat(3, 1fr);">
        <div class="kpi-item">
            <h3 style="margin: 0; color: #2c3e50;">Profitable Vehicles</h3>
            <p style="font-size: 24px; font-weight: bold; color: #27ae60; margin: 10px 0;">
                {{report.data.summary.profitableVehicles}}</p>
        </div>
        <div class="kpi-item">
            <h3 style="margin: 0; color: #2c3e50;">Threshold Met</h3>
            <p style="font-size: 24px; font-weight: bold; color: #f39c12; margin: 10px 0;">
                {{report.data.summary.thresholdMetVehicles}}</p>
        </div>
        <div class="kpi-item">
            <h3 style="margin: 0; color: #2c3e50;">Profitability Rate</h3>
            <p style="font-size: 24px; font-weight: bold; color: #3498db; margin: 10px 0;">{{formatPercentage
                report.data.summary.profitabilityRate}}%</p>
        </div>
    </div>
    {{/if}}

    {{#if report.data.topPerformingVehicles}}
    <h3 style="color: #2c3e50; border-bottom: 2px solid #9b59b6; padding-bottom: 10px;">Top Performing Vehicles</h3>
    <table class="table">
        <thead>
            <tr>
                <th>Vehicle</th>
                <th>Revenue</th>
                <th>Cost</th>
                <th>Profit</th>
                <th>Margin</th>
            </tr>
        </thead>
        <tbody>
            {{#each report.data.topPerformingVehicles}}
            <tr>
                <td>{{this.vehicle}}</td>
                <td>${{formatCurrency this.revenue}}</td>
                <td>${{formatCurrency this.cost}}</td>
                <td class="{{#if (gt this.profit 0)}}positive-profit{{else}}negative-profit{{/if}}">${{formatCurrency
                    this.profit}}</td>
                <td class="{{#if (gt this.profitMargin 0)}}positive-profit{{else}}negative-profit{{/if}}">
                    {{formatPercentage this.profitMargin}}%</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{/if}}

    <div class="footer">
        <p>Generated by Auto Parts Store Reporting System | {{generatedAt}}</p>
        <p>Confidential - For Internal Use Only</p>
    </div>
</body>

</html>