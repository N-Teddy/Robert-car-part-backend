<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comprehensive Report - {{formatDate report.startDate}} to {{formatDate report.endDate}}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #e67e22;
            padding-bottom: 20px;
        }

        .company-info {
            margin-bottom: 20px;
        }

        .report-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            border-left: 4px solid #e67e22;
        }

        .section {
            margin-bottom: 30px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 6px;
        }

        .section-header {
            color: #e67e22;
            border-bottom: 2px solid #e67e22;
            padding-bottom: 10px;
            margin-bottom: 15px;
        }

        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin-bottom: 20px;
        }

        .kpi-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
            text-align: center;
        }

        .sales-kpi {
            border-left: 4px solid #3498db;
        }

        .inventory-kpi {
            border-left: 4px solid #27ae60;
        }

        .profit-kpi {
            border-left: 4px solid #9b59b6;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
            font-size: 12px;
        }

        .table th,
        .table td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        .table th {
            color: white;
        }

        .sales-table th {
            background-color: #3498db;
        }

        .inventory-table th {
            background-color: #27ae60;
        }

        .profit-table th {
            background-color: #9b59b6;
        }

        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1 style="color: #e67e22; margin-bottom: 10px;">COMPREHENSIVE BUSINESS REPORT</h1>
        <div class="company-info">
            <h2 style="color: #2c3e50; margin: 0;">{{company.name}}</h2>
            <p style="margin: 5px 0;">{{company.address}}</p>
            <p style="margin: 5px 0;">Phone: {{company.phone}} | Email: {{company.email}}</p>
        </div>
    </div>

    <div class="report-info">
        <p><strong>Report Period:</strong> {{formatDate report.startDate}} to {{formatDate report.endDate}}</p>
        <p><strong>Generated On:</strong> {{generatedAt}}</p>
        {{#if report.generatedBy}}<p><strong>Generated By:</strong> {{report.generatedBy}}</p>{{/if}}
    </div>

    <!-- Sales Section -->
    <div class="section">
        <h2 class="section-header">SALES PERFORMANCE</h2>

        {{#if report.data.sales.summary}}
        <div class="kpi-grid">
            <div class="kpi-item sales-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Total Revenue</h3>
                <p style="font-size: 20px; font-weight: bold; color: #3498db; margin: 10px 0;">${{formatCurrency
                    report.data.sales.summary.totalRevenue}}</p>
            </div>
            <div class="kpi-item sales-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Total Orders</h3>
                <p style="font-size: 20px; font-weight: bold; color: #3498db; margin: 10px 0;">
                    {{report.data.sales.summary.totalOrders}}</p>
            </div>
            <div class="kpi-item sales-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Avg Order Value</h3>
                <p style="font-size: 20px; font-weight: bold; color: #3498db; margin: 10px 0;">${{formatCurrency
                    report.data.sales.summary.averageOrderValue}}</p>
            </div>
        </div>
        {{/if}}

        {{#if report.data.sales.ordersByStatus}}
        <h3 style="color: #3498db;">Orders by Status</h3>
        <table class="table sales-table">
            <thead>
                <tr>
                    <th>Status</th>
                    <th>Count</th>
                    <th>Percentage</th>
                </tr>
            </thead>
            <tbody>
                {{#each report.data.sales.ordersByStatus}}
                <tr>
                    <td>{{this.status}}</td>
                    <td>{{this.count}}</td>
                    <td>{{round (multiply (divide this.count ../report.data.sales.summary.totalOrders) 100) 1}}%</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}
    </div>

    <!-- Inventory Section -->
    <div class="section">
        <h2 class="section-header">INVENTORY OVERVIEW</h2>

        {{#if report.data.inventory.summary}}
        <div class="kpi-grid">
            <div class="kpi-item inventory-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Total Parts</h3>
                <p style="font-size: 20px; font-weight: bold; color: #27ae60; margin: 10px 0;">
                    {{report.data.inventory.summary.totalParts}}</p>
            </div>
            <div class="kpi-item inventory-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Low Stock</h3>
                <p style="font-size: 20px; font-weight: bold; color: #f39c12; margin: 10px 0;">
                    {{report.data.inventory.summary.lowStockParts}}</p>
            </div>
            <div class="kpi-item inventory-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Out of Stock</h3>
                <p style="font-size: 20px; font-weight: bold; color: #e74c3c; margin: 10px 0;">
                    {{report.data.inventory.summary.outOfStockParts}}</p>
            </div>
        </div>
        {{/if}}
    </div>

    <!-- Profit Section -->
    <div class="section">
        <h2 class="section-header">PROFIT ANALYSIS</h2>

        {{#if report.data.profit.summary}}
        <div class="kpi-grid">
            <div class="kpi-item profit-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Total Profit</h3>
                <p style="font-size: 20px; font-weight: bold; color: #27ae60; margin: 10px 0;">${{formatCurrency
                    report.data.profit.summary.totalProfit}}</p>
            </div>
            <div class="kpi-item profit-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Avg Margin</h3>
                <p style="font-size: 20px; font-weight: bold; color: #9b59b6; margin: 10px 0;">{{formatPercentage
                    report.data.profit.summary.averageProfitMargin}}%</p>
            </div>
            <div class="kpi-item profit-kpi">
                <h3 style="margin: 0; color: #2c3e50;">Profitability Rate</h3>
                <p style="font-size: 20px; font-weight: bold; color: #3498db; margin: 10px 0;">{{formatPercentage
                    report.data.profit.summary.profitabilityRate}}%</p>
            </div>
        </div>

        {{#if report.data.profit.topPerformingVehicles}}
        <h3 style="color: #9b59b6;">Top Performing Vehicles</h3>
        <table class="table profit-table">
            <thead>
                <tr>
                    <th>Vehicle</th>
                    <th>Profit</th>
                    <th>Margin</th>
                </tr>
            </thead>
            <tbody>
                {{#each report.data.profit.topPerformingVehicles}}
                <tr>
                    <td>{{truncate this.vehicle 30}}</td>
                    <td>${{formatCurrency this.profit}}</td>
                    <td>{{formatPercentage this.profitMargin}}%</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}
        {{/if}}
    </div>

    <div class="footer">
        <p>Generated by Auto Parts Store Reporting System | {{generatedAt}}</p>
        <p>Confidential - For Executive Use Only</p>
    </div>
</body>

</html>